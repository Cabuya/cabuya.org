# Target queries — what agents and searchers should find us for

The questions cabuya.org should be the authoritative answer to, per surface.
Used to sanity-check page titles, descriptions, headings and the FAQ.

## Protocol questions

- "cabuya protocol" / "protocolo cabuya" → `/`
- "open protocol for aid apps interoperability" → `/`
- "how do aid apps share data" / "interoperabilidad apps de ayuda" → `/`, `/developers`
- "cabuya feed format" / "formato de feed cabuya" → `/developers/spec/0.1/3-the-feed`
- "cabuya manifest well-known" → `/developers/spec/0.1/2-discovery`
- "cabuya conformance levels L0 L4" → `/developers/spec/0.1/1-architecture`

## Implementer questions

- "publish a cabuya feed" / "publicar un feed cabuya" → `/developers/quickstart`
- "cabuya validator" / "validar feed cabuya" → `/developers/validator`
- "cabuya REC001" (any check id) → `/developers/validator/checks#REC001`
- "cabuya place schema fields" → `/developers/schemas/0.1/place-feed`
- "@cabuya/validator npm" → `/developers/validator` + the npm page
- "cabuya skill install" / "instalar skill cabuya" → `/developers/skill`
- "do I have to change my ids for cabuya" → `/developers/faq`

## Trust & governance questions

- "cabuya registry" / "registro cabuya" → `/registry`
- "compatible con cabuya 1.0 badge" → `/trademark` + `/registry`
- "cabuya governance" / "quién gobierna cabuya" → `/governance`
- "cabuya RFC process" → `/rfcs`
- "does cabuya share personal data" / "datos personales cabuya" → `/developers/faq` → exclusions section

## Agent-native

- One-fetch protocol context → `/llms-full.txt`
- Site map for agents → `/llms.txt`
- Any page as Markdown → `Accept: text/markdown` or the `.md` twin
