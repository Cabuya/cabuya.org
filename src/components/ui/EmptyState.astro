---
/**
 * Cabuya empty-state component — used when a collection-driven listing
 * has no entries (e.g., upcoming events, talks for a speaker without any).
 */
import Illustration from '@/components/editorial/Illustration.astro';

interface Props {
  title: string;
  description?: string;
  ctaLabel?: string;
  ctaHref?: string;
  class?: string;
  /**
   * Which mark sits above the title.
   *
   * `coil` is `MK-02` from the illustration pack — a coiled thread waiting to be
   * used, for the empty states that are a true statement about today rather than
   * a failure. Omitted, the component keeps its generic line icon, which is what
   * the entry means by "the component must render correctly without it".
   */
  mark?: 'coil';
  /**
   * Heading level for the title. Defaults to `h2` because an empty state
   * usually replaces a whole section: when it sits directly under the page
   * `h1`, an `h3` would skip a level.
   */
  headingLevel?: 'h2' | 'h3';
}

const {
  title,
  description,
  ctaLabel,
  ctaHref,
  class: className = '',
  headingLevel = 'h2',
  mark,
} = Astro.props;

const Heading = headingLevel;
---

<div
  class={`flex flex-col items-center justify-center rounded-2xl border border-dashed border-cabuya-border bg-cabuya-bg-elevated/60 px-6 py-12 text-center ${className}`}
>
  {
    mark === 'coil' ? (
      <Illustration id="empty-coil" class="mb-4 h-auto w-28" />
    ) : (
      <svg
        aria-hidden="true"
        class="h-12 w-12 text-cabuya-primary dark:text-cabuya-primary-light mb-4"
        fill="none"
        viewBox="0 0 24 24"
        stroke-width="1.5"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M3.75 12h16.5m-16.5 3.75h16.5M3.75 19.5h16.5M5.625 4.5h12.75a1.875 1.875 0 0 1 0 3.75H5.625a1.875 1.875 0 0 1 0-3.75z"
        />
      </svg>
    )
  }
  <Heading class="text-lg font-semibold text-cabuya">
    {title}
  </Heading>
  {
    description && (
      <p class="mt-2 max-w-md text-cabuya-secondary">
        {description}
      </p>
    )
  }
  {
    ctaLabel && ctaHref && (
      <a
        href={ctaHref}
        class="mt-6 inline-flex items-center gap-2 rounded-full bg-cabuya-fill px-5 py-2.5 text-sm font-semibold text-cabuya-on-fill shadow-sm transition-colors hover:bg-cabuya-fill-strong focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cabuya-primary"
      >
        {ctaLabel}
      </a>
    )
  }
</div>
