---
/**
 * Ecosystem network page — complementary civic apps around the emergency
 * response. Corag is featured as one node; peers are listed by category.
 * Closing band hosts the DailyBot-backed join form.
 */
import EcosystemAppsExplorer from '@/components/ecosystem/EcosystemAppsExplorer.svelte';
import EcosystemJoinForm from '@/components/ecosystem/EcosystemJoinForm.svelte';
import Breadcrumbs from '@/components/ui/Breadcrumbs.astro';
import Eyebrow from '@/components/ui/Eyebrow.astro';
import MainLayout from '@/layouts/MainLayout.astro';
import { APP_URL } from '@/lib/constances';
import { loadEcosystemDirectoryViews } from '@/lib/ecosystem';
import { getUrlPrefix, type Language } from '@/lib/i18n';
import { getTranslations } from '@/lib/translations';

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = getTranslations(lang);
const p = t.ecosystemPage;
const prefix = getUrlPrefix(lang);
const { featured, groups } = await loadEcosystemDirectoryViews(lang);

const breadcrumbs = [
  { label: lang === 'es' ? 'Inicio' : 'Home', href: `${prefix}/` },
  { label: p.title },
];

const explorerLabels = {
  featuredEyebrow: p.featuredEyebrow,
  featuredImageAlt: p.featuredImageAlt,
  featuredImageSrc:
    lang === 'en'
      ? '/images/home/app/app-home-desktop-en-1280.webp'
      : '/images/home/app/app-home-desktop-1280.webp',
  ctaApp: p.ctaApp,
  ctaHow: p.ctaHow,
  howItWorksHref: `${prefix}/how-it-works`,
  directoryEyebrow: p.directoryEyebrow,
  directoryTitle: p.directoryTitle,
  directoryLead: p.directoryLead,
  categories: p.categories,
  categoryLeads: p.categoryLeads,
  whatLabel: p.whatLabel,
  howLabel: p.howLabel,
  visit: p.visit,
  apiDocs: p.apiDocs,
  disclosure: p.disclosure,
  moreInfo: p.moreInfo,
  close: p.close,
  overview: p.overview,
  features: p.features,
  tools: p.tools,
  audience: p.audience,
  coverage: p.coverage,
  limits: p.limits,
  integrations: p.integrations,
  publicApi: p.publicApi,
  publicMcp: p.publicMcp,
  availabilityYes: p.availabilityYes,
  availabilityNo: p.availabilityNo,
  availabilityUnknown: p.availabilityUnknown,
  openApi: p.openApi,
  mcpEndpoint: p.mcpEndpoint,
  developers: p.developers,
  badgeApi: p.badgeApi,
  badgeMcp: p.badgeMcp,
};
---

<MainLayout lang={lang} title={p.title} description={p.description}>
  <!-- HERO -->
  <section
    class="relative overflow-hidden border-b border-cabuya-border bg-cabuya-bg"
  >
    <div
      class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,var(--color-cabuya-primary-soft)_0%,transparent_55%),radial-gradient(ellipse_at_bottom_left,var(--color-cabuya-seedling-soft)_0%,transparent_50%)] opacity-90 dark:opacity-40"
      aria-hidden="true"
    >
    </div>
    <div
      class="pointer-events-none absolute -right-16 top-10 h-64 w-64 rounded-full border border-cabuya-primary/20 sm:h-96 sm:w-96"
      aria-hidden="true"
    >
    </div>
    <div
      class="pointer-events-none absolute -right-4 top-28 h-40 w-40 rounded-full border border-cabuya-accent/30 sm:h-56 sm:w-56"
      aria-hidden="true"
    >
    </div>

    <div class="main-container relative pt-10 pb-4 sm:pt-14">
      <Breadcrumbs items={breadcrumbs} />
    </div>

    <div class="main-container relative pb-16 pt-6 sm:pb-24 sm:pt-10">
      <Eyebrow>{p.eyebrow}</Eyebrow>
      <h1
        class="mt-4 max-w-3xl text-4xl font-bold tracking-tight text-cabuya sm:text-5xl lg:text-6xl"
      >
        {p.headline}
      </h1>
      <p
        class="mt-5 max-w-2xl text-lg leading-relaxed text-cabuya-secondary sm:text-xl"
      >
        {p.lead}
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a
          href={APP_URL}
          class="inline-flex min-h-[48px] items-center justify-center rounded-full bg-cabuya-fill px-7 text-base font-semibold text-cabuya-on-fill transition hover:bg-cabuya-fill-strong"
          rel="noopener noreferrer"
          target="_blank"
        >
          {p.ctaApp}
        </a>
        <a
          href="#ecosystem-directory"
          class="inline-flex min-h-[48px] items-center justify-center rounded-full border border-cabuya-border bg-cabuya-bg/80 px-7 text-base font-semibold text-cabuya transition hover:border-cabuya-primary hover:bg-cabuya-primary-soft"
        >
          {p.ctaDirectory}
        </a>
      </div>
    </div>
  </section>

  <EcosystemAppsExplorer
    client:visible
    lang={lang}
    urlPrefix={prefix}
    featured={featured ?? null}
    groups={groups}
    labels={explorerLabels}
  />

  <!-- JOIN -->
  <section
    id="ecosystem-join"
    class="scroll-mt-24 bg-cabuya-primary-soft/35 dark:bg-cabuya-primary-soft/20"
  >
    <div class="main-container py-14 sm:py-20">
      <div class="mx-auto max-w-3xl">
        <Eyebrow>{p.joinEyebrow}</Eyebrow>
        <h2 class="mt-3 text-3xl font-bold tracking-tight text-cabuya sm:text-4xl">
          {p.joinTitle}
        </h2>
        <p class="mt-4 text-base leading-relaxed text-cabuya-secondary">
          {p.joinLead}
        </p>
        <div class="mt-8 rounded-3xl border border-cabuya-border bg-cabuya-bg p-5 shadow-sm sm:p-8">
          <EcosystemJoinForm client:visible lang={lang} />
        </div>
      </div>
    </div>
  </section>
</MainLayout>
