---
import ShowcaseLayout from '@/layouts/ShowcaseLayout.astro';

/**
 * Design System Showcase — Color.
 *
 * Complements the Brand Book (`/internal/brand/colors`), which documents the
 * palette's *rules*. This page is the **live** view: every swatch renders with
 * the real utility class, and the hex printed under it is read from
 * `getComputedStyle` at runtime rather than hardcoded.
 *
 * That matters. The previous version of this page hardcoded hex strings, and
 * they silently went stale the moment the palette changed — it was still
 * printing the old teal/amber values while rendering the new ones. A page whose
 * job is to verify colors must not be able to lie about them.
 */

interface Token {
  name: string;
  cssVar: string;
  /** Utility that paints the swatch. */
  bgClass: string;
  /** Utility that keeps the label readable on that swatch. */
  textOnClass: string;
  usage: string;
  flag?: 'restricted' | 'no-flip' | 'interactive';
}

interface Group {
  title: string;
  blurb: string;
  tokens: Token[];
}

const groups: Group[] = [
  {
    title: 'Brand — foreground',
    blurb:
      'Flips between themes: forest in light, fique in dark. Text, icons, borders, focus rings.',
    tokens: [
      {
        name: 'Primary',
        cssVar: '--color-cabuya-primary',
        bgClass: 'bg-cabuya-primary',
        textOnClass: 'text-cabuya-on-fill dark:text-cabuya-bg-dark',
        usage: 'The brand color in running context',
      },
      {
        name: 'Primary strong',
        cssVar: '--color-cabuya-primary-strong',
        bgClass: 'bg-cabuya-primary-strong',
        textOnClass: 'text-cabuya-on-fill dark:text-cabuya-bg-dark',
        usage: 'Hover · active · pressed',
      },
      {
        name: 'Primary soft',
        cssVar: '--color-cabuya-primary-soft',
        bgClass: 'bg-cabuya-primary-soft',
        textOnClass: 'text-cabuya',
        usage: 'Tinted surface behind primary content',
      },
      {
        name: 'Primary light',
        cssVar: '--color-cabuya-primary-light',
        bgClass: 'bg-cabuya-primary-light',
        textOnClass: 'text-cabuya-bg-dark',
        usage: 'The dark-mode primary, available in both themes',
        flag: 'no-flip',
      },
    ],
  },
  {
    title: 'Brand — solid fill',
    blurb:
      'Never flips. This is the pair a filled button must use, so it stays readable when the theme changes.',
    tokens: [
      {
        name: 'Fill',
        cssVar: '--color-cabuya-fill',
        bgClass: 'bg-cabuya-fill',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'Background of a filled brand surface',
        flag: 'no-flip',
      },
      {
        name: 'Fill strong',
        cssVar: '--color-cabuya-fill-strong',
        bgClass: 'bg-cabuya-fill-strong',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'Its hover state',
        flag: 'no-flip',
      },
      {
        name: 'On fill',
        cssVar: '--color-cabuya-on-fill',
        bgClass: 'bg-cabuya-on-fill',
        textOnClass: 'text-cabuya-fill',
        usage: 'The label on that surface — 9.54:1 in both themes',
        flag: 'no-flip',
      },
    ],
  },
  {
    title: 'Seedling & fique',
    blurb:
      'Seedling is the pale green that carries display text on dark canvases. Raw fique is decorative only.',
    tokens: [
      {
        name: 'Seedling',
        cssVar: '--color-cabuya-seedling',
        bgClass: 'bg-cabuya-seedling',
        textOnClass: 'text-cabuya-fill',
        usage: 'Display text on Night; pale brand surfaces',
        flag: 'no-flip',
      },
      {
        name: 'Seedling soft',
        cssVar: '--color-cabuya-seedling-soft',
        bgClass: 'bg-cabuya-seedling-soft',
        textOnClass: 'text-cabuya-fill',
        usage: 'Lighter brand surface',
        flag: 'no-flip',
      },
      {
        name: 'Accent',
        cssVar: '--color-cabuya-accent',
        bgClass: 'bg-cabuya-accent',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'Hairlines, shapes, large display type — NEVER body text',
        flag: 'restricted',
      },
      {
        name: 'Accent strong',
        cssVar: '--color-cabuya-accent-strong',
        bgClass: 'bg-cabuya-accent-strong',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'The accent when it must carry readable text',
      },
    ],
  },
  {
    title: 'Surfaces',
    blurb: 'Page, elevated and brand-tinted grounds.',
    tokens: [
      {
        name: 'Bg',
        cssVar: '--color-cabuya-bg',
        bgClass: 'bg-cabuya-bg',
        textOnClass: 'text-cabuya',
        usage: 'Page background',
      },
      {
        name: 'Bg elevated',
        cssVar: '--color-cabuya-bg-elevated',
        bgClass: 'bg-cabuya-bg-elevated',
        textOnClass: 'text-cabuya',
        usage: 'Cards, panels, header',
      },
      {
        name: 'Bg brand',
        cssVar: '--color-cabuya-bg-brand',
        bgClass: 'bg-cabuya-bg-brand',
        textOnClass: 'text-cabuya',
        usage: 'Brand-tinted section',
      },
      {
        name: 'Bg brand strong',
        cssVar: '--color-cabuya-bg-brand-strong',
        bgClass: 'bg-cabuya-bg-brand-strong',
        textOnClass: 'text-cabuya',
        usage: 'Stronger brand section',
      },
      {
        name: 'Bg dark',
        cssVar: '--color-cabuya-bg-dark',
        bgClass: 'bg-cabuya-bg-dark',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'Always-dark canvas — identical in both themes',
        flag: 'no-flip',
      },
    ],
  },
  {
    title: 'Text',
    blurb: 'Nothing lighter than “muted” is permitted for body copy.',
    tokens: [
      {
        name: 'Text',
        cssVar: '--color-cabuya-text',
        bgClass: 'bg-cabuya-text',
        textOnClass: 'text-cabuya-bg',
        usage: 'Body copy',
      },
      {
        name: 'Text secondary',
        cssVar: '--color-cabuya-text-secondary',
        bgClass: 'bg-cabuya-text-secondary',
        textOnClass: 'text-cabuya-bg',
        usage: 'Supporting copy',
      },
      {
        name: 'Text muted',
        cssVar: '--color-cabuya-text-muted',
        bgClass: 'bg-cabuya-text-muted',
        textOnClass: 'text-cabuya-bg',
        usage: 'The lightest permitted body text',
      },
    ],
  },
  {
    title: 'Borders',
    blurb:
      'Only “interactive” clears the 3:1 requirement for meaningful boundaries.',
    tokens: [
      {
        name: 'Border',
        cssVar: '--color-cabuya-border',
        bgClass: 'bg-cabuya-border',
        textOnClass: 'text-cabuya',
        usage: 'Decorative hairlines, dividers',
      },
      {
        name: 'Border strong',
        cssVar: '--color-cabuya-border-strong',
        bgClass: 'bg-cabuya-border-strong',
        textOnClass: 'text-cabuya',
        usage: 'Decorative emphasis',
      },
      {
        name: 'Border interactive',
        cssVar: '--color-cabuya-border-interactive',
        bgClass: 'bg-cabuya-border-interactive',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'Inputs, selects, control edges',
        flag: 'interactive',
      },
    ],
  },
  {
    title: 'Status',
    blurb:
      'These sit on page or soft-chip grounds — never on a filled brand surface.',
    tokens: [
      {
        name: 'Success',
        cssVar: '--color-cabuya-success',
        bgClass: 'bg-cabuya-success',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'entregado · con evidencia · completado',
      },
      {
        name: 'Success soft',
        cssVar: '--color-cabuya-success-soft',
        bgClass: 'bg-cabuya-success-soft',
        textOnClass: 'text-cabuya-success',
        usage: 'Its chip background',
      },
      {
        name: 'Warning',
        cssVar: '--color-cabuya-warning',
        bgClass: 'bg-cabuya-warning',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'en organización · pendiente',
      },
      {
        name: 'Warning soft',
        cssVar: '--color-cabuya-warning-soft',
        bgClass: 'bg-cabuya-warning-soft',
        textOnClass: 'text-cabuya-warning',
        usage: 'Its chip background',
      },
      {
        name: 'Info',
        cssVar: '--color-cabuya-info',
        bgClass: 'bg-cabuya-info',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'informativo · neutral',
      },
      {
        name: 'Info soft',
        cssVar: '--color-cabuya-info-soft',
        bgClass: 'bg-cabuya-info-soft',
        textOnClass: 'text-cabuya-info',
        usage: 'Its chip background',
      },
      {
        name: 'Danger',
        cssVar: '--color-cabuya-danger',
        bgClass: 'bg-cabuya-danger',
        textOnClass: 'text-cabuya-on-fill',
        usage: 'urgente · error',
      },
      {
        name: 'Danger soft',
        cssVar: '--color-cabuya-danger-soft',
        bgClass: 'bg-cabuya-danger-soft',
        textOnClass: 'text-cabuya-danger',
        usage: 'Its chip background',
      },
    ],
  },
];

const flagLabel = {
  restricted: 'decorative only',
  'no-flip': 'no flip',
  interactive: '3:1 boundary',
} as const;

const flagClass = {
  restricted: 'bg-cabuya-danger-soft text-cabuya-danger',
  'no-flip': 'bg-cabuya-info-soft text-cabuya-info',
  interactive: 'bg-cabuya-success-soft text-cabuya-success',
} as const;

const totalTokens = groups.reduce((n, g) => n + g.tokens.length, 0);
---

<ShowcaseLayout
  title="Color"
  description="Every design token, rendered live. Hex values are read from the computed styles, so they cannot go stale."
  section="ui"
>
  <div class="space-y-12">
    <header class="space-y-3">
      <h1 class="text-3xl font-bold text-cabuya sm:text-4xl">Color</h1>
      <p class="max-w-3xl text-cabuya-secondary">
        {totalTokens} tokens, declared once in <code>src/styles/global.css</code>
         via Tailwind 4's <code>@theme</code>. Every swatch below is painted by the
        real utility class, and the value printed under it is read from
        <code>getComputedStyle</code> when the page loads — so this page shows what
        the CSS actually produces, not what someone typed here.
      </p>
      <p class="max-w-3xl text-sm text-cabuya-text-muted">
        Rules and measured contrast ratios live in the Brand Book:
        <a href="/internal/brand/colors" class="text-cabuya-primary underline"
          >Brand → Colors</a
        >. Implementation contract: <code>docs/DESIGN.md</code>.
      </p>
    </header>

    <section
      class="rounded-[18px] border border-cabuya-border bg-cabuya-bg-brand p-6"
    >
      <h2 class="font-display text-lg font-semibold text-cabuya">
        Toggle the theme while you read this page
      </h2>
      <p class="mt-2 text-sm text-cabuya-secondary">
        In light mode the brand foreground is <strong>forest</strong>; in dark mode it
        becomes <strong>fique</strong>, because forest on Night measures about 1.9:1
        and is unreadable. Both are official colours. Every value below re-reads itself when
        the theme changes, so you can watch the whole system flip and confirm
        nothing breaks.
      </p>
    </section>

    {
      groups.map((group) => (
        <section class="space-y-4">
          <div>
            <h2 class="font-display text-2xl font-bold text-cabuya">
              {group.title}
            </h2>
            <p class="mt-1 text-sm text-cabuya-secondary">{group.blurb}</p>
          </div>
          <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {group.tokens.map((token) => (
              <div class="overflow-hidden rounded-[18px] border border-cabuya-border bg-cabuya-bg-elevated shadow-cabuya-sm">
                <div
                  class={`flex h-24 items-end justify-between p-4 ${token.bgClass} ${token.textOnClass}`}
                >
                  <span class="font-display text-base font-semibold">
                    {token.name}
                  </span>
                  <code
                    class="text-xs font-semibold tabular-nums"
                    data-swatch-value={token.cssVar}
                  >
                    …
                  </code>
                </div>
                <div class="space-y-2 p-4">
                  <div class="flex flex-wrap items-center gap-2">
                    <code class="text-xs text-cabuya-text-muted">
                      {token.cssVar}
                    </code>
                    {token.flag && (
                      <span
                        class={`rounded-full px-2 py-0.5 text-[11px] font-semibold ${flagClass[token.flag]}`}
                      >
                        {flagLabel[token.flag]}
                      </span>
                    )}
                  </div>
                  <p class="text-sm text-cabuya-secondary">{token.usage}</p>
                </div>
              </div>
            ))}
          </div>
        </section>
      ))
    }

    <section class="space-y-4">
      <h2 class="font-display text-2xl font-bold text-cabuya">Short aliases</h2>
      <p class="max-w-3xl text-sm text-cabuya-secondary">
        Two tokens are aliases rather than distinct colors. They exist because
        these two are used constantly, and they re-export via <code>var()</code>
         so they inherit the light/dark flip with no extra rule.
      </p>
      <div class="overflow-x-auto">
        <table class="w-full min-w-[34rem] text-left text-sm">
          <thead class="border-b border-cabuya-border">
            <tr class="text-xs uppercase tracking-wide text-cabuya-text-muted">
              <th class="py-2 pr-4">Alias</th>
              <th class="py-2 pr-4">Resolves to</th>
              <th class="py-2">Computed value</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-cabuya-border">
            <tr>
              <td class="py-3 pr-4"><code>--color-cabuya</code> · <code>text-cabuya</code></td>
              <td class="py-3 pr-4"><code>--color-cabuya-text</code></td>
              <td class="py-3">
                <code class="text-xs tabular-nums" data-swatch-value="--color-cabuya">…</code>
              </td>
            </tr>
            <tr>
              <td class="py-3 pr-4"><code>--color-cabuya-secondary</code> · <code>text-cabuya-secondary</code></td>
              <td class="py-3 pr-4"><code>--color-cabuya-text-secondary</code></td>
              <td class="py-3">
                <code class="text-xs tabular-nums" data-swatch-value="--color-cabuya-secondary">…</code>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="font-display text-2xl font-bold text-cabuya">
        The two rules that get broken
      </h2>
      <div class="grid gap-4 md:grid-cols-2">
        <div
          class="rounded-[18px] border border-cabuya-danger/30 bg-cabuya-danger-soft p-5"
        >
          <p class="font-display font-semibold text-cabuya-danger">
            ❌ <code>bg-cabuya-primary</code> + <code>text-white</code>
          </p>
          <p class="mt-2 text-sm text-cabuya-secondary">
            Fine in light mode, and roughly 2:1 in dark — because primary becomes
            fique and white sits on top of it. Toggle the theme and watch it fail.
          </p>
          <span
            class="mt-3 inline-flex rounded-full bg-cabuya-primary px-4 py-2 text-sm font-semibold text-white"
          >
            Publicar un feed
          </span>
        </div>
        <div
          class="rounded-[18px] border border-cabuya-success/30 bg-cabuya-success-soft p-5"
        >
          <p class="font-display font-semibold text-cabuya-success">
            ✅ <code>bg-cabuya-fill</code> + <code>text-cabuya-on-fill</code>
          </p>
          <p class="mt-2 text-sm text-cabuya-secondary">
            9.54:1 in both themes. A primary button needs no dark variant at all.
          </p>
          <span
            class="mt-3 inline-flex rounded-full bg-cabuya-fill px-4 py-2 text-sm font-semibold text-cabuya-on-fill"
          >
            Publicar un feed
          </span>
        </div>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="font-display text-2xl font-bold text-cabuya">In context</h2>
      <div class="grid gap-4 md:grid-cols-2">
        <div
          class="space-y-3 rounded-[18px] border border-cabuya-border bg-cabuya-bg-elevated p-6 shadow-cabuya-sm"
        >
          <p class="font-display text-xl font-semibold text-cabuya">
            Card on an elevated surface
          </p>
          <p class="text-cabuya-secondary">Supporting copy in the secondary token.</p>
          <p class="text-sm text-cabuya-text-muted">
            Muted copy — the lightest permitted body text.
          </p>
          <div class="flex flex-wrap gap-2 pt-2">
            <span class="rounded-full bg-cabuya-success-soft px-3 py-1 text-sm font-medium text-cabuya-success">
              Con evidencia
            </span>
            <span class="rounded-full bg-cabuya-warning-soft px-3 py-1 text-sm font-medium text-cabuya-warning">
              En organización
            </span>
            <span class="rounded-full bg-cabuya-danger-soft px-3 py-1 text-sm font-medium text-cabuya-danger">
              Urgente
            </span>
          </div>
        </div>
        <div class="space-y-3 rounded-[18px] bg-cabuya-bg-dark p-6">
          <p class="font-display text-xl font-semibold text-cabuya-on-fill">
            Always-dark canvas
          </p>
          <p class="text-cabuya-seedling">
            Identical in both themes, so a hero can keep the forest identity
            regardless of the reader's preference.
          </p>
          <label class="block text-sm font-medium text-cabuya-on-fill" for="demo-input">
            An input, with the interactive border
          </label>
          <input
            id="demo-input"
            class="w-full rounded-[12px] border border-cabuya-border-interactive bg-cabuya-bg-elevated px-4 py-2.5 text-cabuya placeholder:text-cabuya-text-muted"
            placeholder="border-cabuya-border-interactive"
          />
        </div>
      </div>
    </section>
  </div>
</ShowcaseLayout>

<script>
  /*
   * Print each token's *computed* value, so this page can never disagree with
   * the stylesheet. Re-runs whenever the theme class changes, which is what
   * makes the light/dark flip inspectable rather than something you have to
   * take on faith.
   */
  const toHex = (value: string): string => {
    const m = value.match(/rgba?\(([^)]+)\)/);
    if (!m) return value.trim().toUpperCase();
    const [r, g, b] = m[1].split(',').map((n) => Number.parseFloat(n));
    return `#${[r, g, b].map((n) => Math.round(n).toString(16).padStart(2, '0')).join('').toUpperCase()}`;
  };

  const paint = () => {
    const style = getComputedStyle(document.documentElement);
    for (const el of document.querySelectorAll<HTMLElement>('[data-swatch-value]')) {
      const name = el.dataset.swatchValue;
      if (!name) continue;
      const raw = style.getPropertyValue(name);
      el.textContent = raw ? toHex(raw) : 'undefined';
      if (!raw) el.classList.add('underline', 'decoration-wavy');
    }
  };

  paint();
  new MutationObserver(paint).observe(document.documentElement, {
    attributes: true,
    attributeFilter: ['class'],
  });
</script>
